% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matches.R
\name{team_past_results}
\alias{team_past_results}
\title{A team's past results}
\usage{
team_past_results(team, now = Sys.time(), tz = "America/New_York")
}
\arguments{
\item{team}{A team name, short name, three-letter code, or alias
(e.g. \code{"USA"}, \code{"United States"}, \code{"Korea"}).}

\item{now}{The reference time used to determine whether a match is in
the past. Defaults to \code{\link[=Sys.time]{Sys.time()}}.}

\item{tz}{A single time-zone name (see \code{\link[=OlsonNames]{OlsonNames()}}) used for the
\code{match_date} and \code{kickoff} display columns. Defaults to
\code{"America/New_York"} (US Eastern).}
}
\value{
A tibble in the same shape as \code{\link[=team_schedule]{team_schedule()}}, ordered with
the most recent match last.
}
\description{
Returns matches whose scheduled date is in the past. Matches with
\code{status = "FINISHED"} and missing scores get a \code{score_display} of
\code{"no score available yet"}; live matches show \code{"in progress"}.
}
\examples{
\dontrun{
team_past_results("Argentina")
}
}
