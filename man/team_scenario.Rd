% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standings.R
\name{team_scenario}
\alias{team_scenario}
\title{What a team needs to reach the top two of its group}
\usage{
team_scenario(team, matches = all_matches(), teams = list_teams())
}
\arguments{
\item{team}{A team name, short name, three-letter code, or alias.}

\item{matches}{A matches tibble. Defaults to \code{\link[=all_matches]{all_matches()}}.}

\item{teams}{A teams tibble. Defaults to \code{\link[=list_teams]{list_teams()}}; used to attach the
three-letter code (\code{tla}).}
}
\value{
A tibble: \code{team}, \code{group_letter}, \code{scenario} (the outcome label),
\code{result} (one of \code{"clinched"}, \code{"eliminated"}, \code{"depends"}, or, when no
matches remain, \code{"final: <status>"}). One row per outcome combination.
}
\description{
Breaks down a single team's remaining group match(es) into outcomes and says,
for each, whether that outcome clinches a top-two finish, eliminates the team
from it, or leaves it dependent on other results. Outcomes are from the team's
own perspective (Win / Draw / Loss); when more than one match remains they are
combined (e.g. \code{"Win, Draw"}). The same sound, never-over-claiming logic as
\code{\link[=advancement_status]{advancement_status()}} is used.
}
\examples{
\dontrun{
team_scenario("USA")
}
}
\seealso{
\code{\link[=advancement_status]{advancement_status()}}
}
