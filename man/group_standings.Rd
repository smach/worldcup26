% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standings.R
\name{group_standings}
\alias{group_standings}
\title{Group-stage standings}
\usage{
group_standings(matches = all_matches(), teams = list_teams())
}
\arguments{
\item{matches}{A matches tibble. Defaults to \code{\link[=all_matches]{all_matches()}}.}

\item{teams}{A teams tibble. Defaults to \code{\link[=list_teams]{list_teams()}}; used to attach the
three-letter code (\code{tla}).}
}
\value{
A tibble, one row per team per group, ordered by group then rank.
Columns: \code{group_letter}, \code{rank}, \code{team}, \code{team_id}, \code{tla}, \code{played},
\code{won}, \code{drawn}, \code{lost}, \code{gf}, \code{ga}, \code{gd}, \code{points}, \code{tie_unresolved},
\code{group_complete}.
}
\description{
Computes the group tables from finished group-stage matches, applying the
FIFA 2026 in-group tiebreakers: points, then head-to-head (points, goal
difference, goals) among teams level on points, then overall goal difference
and goals scored. The deepest official tiebreakers (disciplinary/conduct
score and FIFA ranking) are \strong{not} available from the API, so any teams that
remain level after goals are flagged with \code{tie_unresolved = TRUE} rather than
guessed.
}
\details{
Mid-tournament tables are provisional: they reflect only the matches played
so far. A group is final once \code{group_complete} is \code{TRUE}.
}
\examples{
\dontrun{
group_standings()
}
}
