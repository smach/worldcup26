% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chat_data.R
\name{chat_data}
\alias{chat_data}
\title{Build the flat matches table used by the chat dashboard}
\usage{
chat_data(
  matches = all_matches(),
  teams = list_teams(),
  today = eastern_today()
)
}
\arguments{
\item{matches}{A matches tibble. Defaults to \code{\link[=all_matches]{all_matches()}}.}

\item{teams}{A teams tibble. Defaults to \code{\link[=list_teams]{list_teams()}}. Used to attach
three-letter codes (\code{home_tla}, \code{away_tla}) to each row.}

\item{today}{The reference date for the \code{is_today} flag and for
computing pastness/upcomingness. A US Eastern date (to match
\code{match_date}, which is in US Eastern). Defaults to today's date in
US Eastern.}
}
\value{
A tibble of matches.
}
\description{
Returns a denormalised data frame with one row per match and a set of
derived columns that make natural-language SQL queries straightforward
(today flags, three-letter codes, group letter, finished/upcoming
booleans, etc.).
}
\details{
This is the same data the chat in \code{\link[=worldcup26_chat]{worldcup26_chat()}} sees. You can
call it directly if you want to inspect what the LLM is working with.
}
\examples{
\dontrun{
chat_data()
}
}
