% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chat_app.R
\name{worldcup26_chat}
\alias{worldcup26_chat}
\title{Launch the natural-language chat dashboard}
\usage{
worldcup26_chat(
  data = chat_data(),
  client = NULL,
  model = "claude-sonnet-4-6",
  greeting = chat_default_greeting(),
  ...
)
}
\arguments{
\item{data}{The data frame to chat with. Defaults to \code{\link[=chat_data]{chat_data()}}.}

\item{client}{An \code{ellmer::Chat} object, a function that takes a
\code{system_prompt} and returns an \code{ellmer::Chat}, or a model string
like \code{"anthropic/claude-sonnet-4-6"}. When \code{NULL} (the default),
builds a Claude client using \code{model}.}

\item{model}{Model identifier passed to \code{\link[ellmer:chat_anthropic]{ellmer::chat_anthropic()}}.
Ignored if \code{client} is supplied. Defaults to \code{"claude-sonnet-4-6"}.}

\item{greeting}{Optional Markdown greeting. Defaults to a built-in
greeting with example questions.}

\item{...}{Additional arguments forwarded to
\code{\link[querychat:querychat_app]{querychat::querychat_app()}}.}
}
\value{
Invisibly returns the running app (called for its side effect).
}
\description{
Starts a Shiny app powered by the
\href{https://github.com/posit-dev/querychat}{querychat} package. You can
ask questions about the World Cup schedule and results in plain
English (e.g. \emph{"When is Canada's next game?"}, \emph{"Show me all matches
on June 15"}, \emph{"Which teams are in Group D?"}) and the app translates
them to SQL over a tidy \code{matches} table.
}
\details{
The app uses \href{https://www.anthropic.com}{Anthropic Claude} by default.
You must have an \code{ANTHROPIC_API_KEY} environment variable set (e.g. in
\verb{~/.Renviron}). Get a key at \url{https://console.anthropic.com}.
}
\examples{
\dontrun{
worldcup26_chat()
}
}
