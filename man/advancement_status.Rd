% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standings.R
\name{advancement_status}
\alias{advancement_status}
\title{Per-team advancement status for the top-two race}
\usage{
advancement_status(matches = all_matches(), teams = list_teams())
}
\arguments{
\item{matches}{A matches tibble. Defaults to \code{\link[=all_matches]{all_matches()}}.}

\item{teams}{A teams tibble. Defaults to \code{\link[=list_teams]{list_teams()}}; used to attach the
three-letter code (\code{tla}).}
}
\value{
A tibble: \code{group_letter}, \code{team}, \code{team_id}, \code{top2_status},
\code{group_complete}.
}
\description{
Classifies every group-stage team as \code{"won_group"}, \code{"clinched_top2"},
\code{"eliminated_top2"}, or \code{"alive"} for the race to finish in the top two of
its group (which guarantees a Round-of-32 place). The verdict is computed by
enumerating every win/draw/loss combination of the group's remaining matches.
}
\details{
To stay sound it never over-claims: a "clinched"/"won" verdict assumes the
team \strong{loses} every points tiebreak, and an "eliminated" verdict assumes it
\strong{wins} every tiebreak. Teams that are in fact decided only on goal
difference therefore show as \code{"alive"} until the maths is unambiguous. The
third-place ("best 8") route is handled separately by \code{\link[=third_place_table]{third_place_table()}}.
}
\examples{
\dontrun{
advancement_status()
}
}
\seealso{
\code{\link[=group_standings]{group_standings()}}, \code{\link[=third_place_table]{third_place_table()}}, \code{\link[=team_scenario]{team_scenario()}}
}
