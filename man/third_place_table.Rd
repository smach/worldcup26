% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standings.R
\name{third_place_table}
\alias{third_place_table}
\title{Cross-group ranking of the third-placed teams}
\usage{
third_place_table(matches = all_matches(), teams = list_teams())
}
\arguments{
\item{matches}{A matches tibble. Defaults to \code{\link[=all_matches]{all_matches()}}.}

\item{teams}{A teams tibble. Defaults to \code{\link[=list_teams]{list_teams()}}; used to attach the
three-letter code (\code{tla}).}
}
\value{
A tibble, one row per group (12 when the full schedule is loaded):
\code{position}, \code{group_letter}, \code{team}, \code{team_id}, \code{played}, \code{points}, \code{gd},
\code{gf}, \code{currently_advancing}, \code{in_contention}, \code{provisional}.
}
\description{
Ranks the current third-placed team from each group to show the race for the
eight "best third-placed" Round-of-32 places. Ranking criteria (FIFA 2026,
no head-to-head since these teams come from different groups): points, goal
difference, goals scored. While the group stage is in progress the table is
\strong{provisional} — both which team is third and its record can still change —
so \code{currently_advancing} is a snapshot, not a guarantee, and borderline rows
are flagged \code{in_contention}. It becomes final once every group is complete.
}
\examples{
\dontrun{
third_place_table()
}
}
\seealso{
\code{\link[=group_standings]{group_standings()}}, \code{\link[=advancement_status]{advancement_status()}}
}
